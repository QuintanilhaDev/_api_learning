pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building the App"
                echo 'npm rebuild'
                echo 'npm start'
            }
        }
    }
}